apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  selector:
    app: postgres  # <-- phải trùng với label của pod postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432